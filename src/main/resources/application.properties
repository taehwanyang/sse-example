spring.application.name=sse-example
# 서버 포트
server.port=8080

# Redis 설정
spring.data.redis.host=localhost
spring.data.redis.port=6379

# SSE 설정
app.sse.timeout=3600000

# Redis Pub/Sub 채널
app.redis.channel=notification:broadcast